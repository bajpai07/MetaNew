# MetaShop VTON Backend

Phase 1 foundation for the Virtual Try-On API.

## How to run locally

1. Install dependencies:
```bash
pip install -r requirements.txt
```

2. Start the server:
```bash
uvicorn main:app --reload
```
